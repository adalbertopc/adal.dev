---
import BlogCard from "./blog-card.astro";

const { posts } = Astro.props;
---

<div class="grid gap-4">
  {
    posts?.map(
      ({ slug, data: { title, createdAt, description, readingTime } }) => (
        <BlogCard
          title={title}
          createdAt={createdAt}
          description={description}
          slug={slug}
          readingTime={readingTime}
        />
      )
    )
  }
</div>
