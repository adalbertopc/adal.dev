---
import ProjectCard from "./project-card.astro";

const { projects } = Astro.props;
---

<div class="grid mb-8 sm:grid-cols-2 gap-4">
  {projects?.map((project) => <ProjectCard {...project} />)}
</div>
